<div class="panel-body">
    <!-- ここからフォーム -->
    <%= form_for @list do |f| %>
       <h1>FollowBox</h1>
        <!-- listのnameを記入 -->
        <div class="field">
        <p>
          <%= f.label :"フォローボックスid" %>
          <%= f.text_field :name %>
        </p>
        </div>
        <!-- list作成ボタン -->
        <div class="actions">
          <%= f.submit "新しいフォローボックスを作成する",class:"btn btn-success" %>
        </div>
    <% end %>
</div>
    <p><%= link_to "TOPへ戻る", lists_path %></p>
